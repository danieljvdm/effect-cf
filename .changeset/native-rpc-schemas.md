---
"effect-cf": minor
---

Add `Worker.native(schema)` and `DurableObject.native(schema)` for RPC arguments and results that Cloudflare must transport without JSON encoding, including byte streams, requests, and responses.
