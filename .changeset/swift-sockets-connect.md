---
"effect-cf": minor
---

Add Effect-native support for Cloudflare Socket Workers: inbound Worker and Durable Object `connect` handlers, TCP forwarding through service bindings and Durable Object stubs, byte-stream `Stream` and `Sink` adapters, resource-safe scoped socket lifecycle and STARTTLS helpers, and the low-level `DurableObjectState.container` lifecycle, process, snapshot, interception, HTTP, and TCP APIs. Raise the Effect 4 beta peer floor to `4.0.0-beta.103`.
