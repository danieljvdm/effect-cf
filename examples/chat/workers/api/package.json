{
  "name": "chat-api-worker",
  "version": "0.0.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev -c wrangler.jsonc -c ../../workers/analytics/wrangler.jsonc -c ../../durable-objects/chat-room/wrangler.jsonc",
    "build": "tsc --noEmit && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@effect-cf/example-contracts": "workspace:*",
    "effect": "catalog:",
    "effect-cf": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite-plus": "catalog:"
  }
}
