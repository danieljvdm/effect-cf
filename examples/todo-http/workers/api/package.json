{
  "name": "todo-http-api-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev -c wrangler.jsonc --port 8787",
    "build": "tsc --noEmit && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@effect-cf/todo-http-domain": "workspace:*",
    "effect": "catalog:",
    "effect-cf": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260421.1",
    "typescript": "~6.0.2",
    "vite-plus": "catalog:"
  }
}
