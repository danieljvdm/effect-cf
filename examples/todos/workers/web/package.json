{
  "name": "todos-web-worker",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vp run todos-web#build && wrangler dev -c wrangler.jsonc -c ../api/wrangler.jsonc --port 8788",
    "build": "vp run todos-web#build && tsc --noEmit && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@effect-cf/todos-domain": "workspace:*",
    "effect": "catalog:",
    "effect-cf": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260421.1",
    "typescript": "~6.0.2",
    "vite-plus": "catalog:"
  }
}
